module.exports = { name: 'setpp', aliases: ['setbotpp', 'setprofilepic'], category: 'owner', ownerOnly: true, execute: async (sock, m, { reply }) => { const q = m.quoted || m; const image = q.download ? await q.download().catch(() => null) : null; if (!image || !(q.mtype || '').includes('image')) return reply('Reply to an image.'); try { await sock.updateProfilePicture(sock.user.id, image); return reply('Profile picture updated.'); } catch (error) { return reply(`Profile picture update failed: ${error.message}`); } } };
