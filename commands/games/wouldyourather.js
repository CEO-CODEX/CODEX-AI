module.exports = { name: 'wouldyourather', aliases: ['wyr'], category: 'Games', description: 'Play would you rather', async execute(bot, m) { const choices = ['Would you rather fly or become invisible?', 'Would you rather read minds or see the future?', 'Would you rather live by the sea or in the mountains?', 'Would you rather always be early or always be lucky?']; return m.reply(`🎯 ${choices[Math.floor(Math.random() * choices.length)]}`); } };
