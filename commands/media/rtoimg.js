const { quotedMessage, mimeOf, download, tempDir, ffmpeg, cleanup, fs, path } = require('../../library/media');
module.exports = { name: 'rtoimg', alias: ['rtoimage', 'rtovideo', 'rtovid', 'r2img'], category: 'Media', desc: 'Convert a round sticker to media', execute: async (sock, m, { reply }) => { const q = quotedMessage(m), mime = mimeOf(q); if (!/webp/.test(mime)) return reply('Reply to a round sticker.'); const dir=tempDir(), input=path.join(dir,`rto-${Date.now()}.webp`), output=path.join(dir,`rto-${Date.now()}.png`); try { fs.writeFileSync(input, await download(q)); await ffmpeg(input, output, []); await sock.sendMessage(m.chat, { image: fs.readFileSync(output), mimetype:'image/png' }, { quoted:m }); } catch(e) { return reply(`Failed: ${e.message}`); } finally { cleanup(input,output); } } };
